diff --git a/eti-cmdline/src/eti-handling/fic-handler.cpp b/eti-cmdline/src/eti-handling/fic-handler.cpp
index a8df864..2fccac3 100755
--- a/eti-cmdline/src/eti-handling/fic-handler.cpp
+++ b/eti-cmdline/src/eti-handling/fic-handler.cpp
@@ -178,11 +178,71 @@ int16_t inputCount	= 0;
 	for (i = 0; i < 3072 + 24; i ++)
 	   if (punctureTable [i])
 	      viterbiBlock [i] = ofdm_input [inputCount ++];
+
+	// ---- dab-rs Slice-7 diagnostic dumps (env-gated) ----
+	// DAB_RS_ORACLE_VITERBI_IN  → per-ficBlock depunctured codeword
+	//     (3096 × i16, in viterbiBlock-internal order — same as the
+	//     mother-codeword layout dab-rs's FicProtection assembles).
+	// DAB_RS_ORACLE_VITERBI_OUT → per-ficBlock 768 hard bits emitted
+	//     by viterbiSpiral, BEFORE the PRBS energy-dispersal XOR.
+	// Header per record: u32 frame_idx (1-based, increments on ficno==0),
+	// u32 ficno (0..3).  Both files are fflush'd per ficBlock so they
+	// are visible mid-run.
+	{
+	   static FILE *   _vi_fp        = nullptr;
+	   static FILE *   _vo_fp        = nullptr;
+	   static int      _vd_init      = 0;
+	   static uint32_t _vd_frame_idx = 0;
+	   if (!_vd_init) {
+	      const char *p;
+	      if ((p = getenv ("DAB_RS_ORACLE_VITERBI_IN")) != nullptr && *p) {
+	         _vi_fp = fopen (p, "wb");
+	         if (_vi_fp == nullptr)
+	            fprintf (stderr, "dab-rs slice7 in: fopen('%s') failed\n", p);
+	         else
+	            fprintf (stderr, "dab-rs slice7 in: writing viterbi input to %s\n", p);
+	      }
+	      if ((p = getenv ("DAB_RS_ORACLE_VITERBI_OUT")) != nullptr && *p) {
+	         _vo_fp = fopen (p, "wb");
+	         if (_vo_fp == nullptr)
+	            fprintf (stderr, "dab-rs slice7 out: fopen('%s') failed\n", p);
+	         else
+	            fprintf (stderr, "dab-rs slice7 out: writing viterbi output to %s\n", p);
+	      }
+	      _vd_init = 1;
+	   }
+	   if (ficno == 0) _vd_frame_idx++;
+	   if (_vi_fp != nullptr) {
+	      uint32_t hdr [2] = { _vd_frame_idx, (uint32_t) ficno };
+	      fwrite (hdr,          sizeof (uint32_t), 2,           _vi_fp);
+	      fwrite (viterbiBlock, sizeof (int16_t),  3072 + 24,   _vi_fp);
+	      fflush (_vi_fp);
+	   }
+	}
+
 /**
   *	Now we have the full word ready for deconvolution
   *	deconvolution is according to DAB standard section 11.2
   */
 	deconvolve (viterbiBlock, bitBuffer_out);
+
+	{
+	   static FILE *   _vo2_fp        = nullptr;
+	   static int      _vo2_init      = 0;
+	   static uint32_t _vo2_frame_idx = 0;
+	   if (!_vo2_init) {
+	      const char *p = getenv ("DAB_RS_ORACLE_VITERBI_OUT");
+	      if (p != nullptr && *p) _vo2_fp = fopen (p, "wb");
+	      _vo2_init = 1;
+	   }
+	   if (ficno == 0) _vo2_frame_idx++;
+	   if (_vo2_fp != nullptr) {
+	      uint32_t hdr [2] = { _vo2_frame_idx, (uint32_t) ficno };
+	      fwrite (hdr,           sizeof (uint32_t), 2,    _vo2_fp);
+	      fwrite (bitBuffer_out, sizeof (uint8_t),  768,  _vo2_fp);
+	      fflush (_vo2_fp);
+	   }
+	}
 /**
   *	if everything worked as planned, we now have a
   *	768 bit vector containing three FIB's
